{
    "name": "secret-santa-application",
    "version": "0.0.3",
    "scripts": {
        "client": "cd client && yarn start",
        "server": "nodemon server/index.js",
        "dev": "cd server && concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
    }
}
